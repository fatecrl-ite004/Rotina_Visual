import 'package:flutter/material.dart';

class PictogramasScreen extends StatelessWidget {
  const PictogramasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pictogramas')),
      body: const Center(child: Text('Galeria de pictogramas em desenvolvimento')),
    );
  }
}